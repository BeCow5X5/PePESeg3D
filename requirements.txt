# PePESeg3D — verified stack: Python 3.11, CUDA 11.8
# Install torch/torchvision for your CUDA build first, e.g.
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu118

numpy==1.26.4
scipy==1.17.0
scikit-learn==1.6.1
opencv-python==4.9.0.80
Pillow==10.4.0
matplotlib==3.10.8
pandas==2.2.3
imageio==2.37.3
tqdm==4.67.1
plyfile==1.1.3
kornia==0.8.2
hdbscan==0.8.40
joblib==1.5.0
lpips==0.1.4
open3d==0.19.0

# --- GroundingDINO (LERF-Mask text-prompt evaluation) ---
# the released checkpoint carries `bert.embeddings.position_ids`, which newer
# transformers releases reject — keep this trio pinned.
transformers==4.30.2
tokenizers==0.13.3
huggingface_hub==0.16.4
timm==1.0.26
addict==2.4.0
yacs==0.1.8
supervision==0.25.1
pycocotools==2.0.8

# --- optional ---
# pytorch3d==0.7.8   # required only by eval_spin_nerf.py / eval_nvos.py
